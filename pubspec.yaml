name: bolter
version: 1.0.7
description: Simplest mutable state manager powered by dart streams and it's extensions
homepage: https://github.com/Renesanse/bolter

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  equatable: ^1.2.5
  stream_transform: ^1.2.0

dev_dependencies:
  lint: ^1.3.0
  test: ^1.15.4
